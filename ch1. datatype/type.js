// type
var intNum = 10;

var floatNum = 0.1;

var singQuoteStr = 'sing quote string';
var doubleQuoteStr = 'double quote string';
var singleChar = 'a';

var boolVar ='a';

var emptyVar;

var nullVar = null;

console.log(
    typeof intNum,
    typeof floatNum,
    typeof singQuoteStr,
    typeof doubleQuoteStr,
    typeof singleChar,
    typeof boolVar,
    typeof emptyVar,
    typeof nullVar
);

// operator
// Number밖에 없으므로 실수로 표현되므로 주의
var num = 5/2;
console.log(num);
console.log(Math.floor(num));
singQuoteStr[0]='S'
console.log(singQuoteStr[1], singQuoteStr);